# Input ZIP Notes

The uploaded ZIP contained multiple previous directions:

- MPU-only PoC direction,
- integration-stack direction,
- failure-framework v2 direction,
- unified v0.2 documentation.

This v3 package intentionally consolidates them into one clean direction:

```text
Guard Blocks first.
Failure pipeline underneath.
Existing libraries as accelerators.
RTOS/MPU as optional backends.
```

Older duplicate prompt names were not carried forward to avoid Codex confusion.
